package com.simplecash.dal.repository;

import com.simplecash.object.DbInfo;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 *
 */
public interface DbInfoRepository extends JpaRepository<DbInfo, Long> {
}
